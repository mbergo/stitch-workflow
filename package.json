{
  "name": "stitch-design-workflow",
  "version": "1.0.0",
  "description": "MCP server orchestrating Google Stitch → Claude Code / VS Code / Antigravity design-to-code pipeline",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@google/stitch-sdk": "^0.4.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
